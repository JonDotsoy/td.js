import expect from 'expect'
import {templatePack} from '../lib/core/generator'

describe('libreries', () => {
	describe('generator', () => {
		it('templatePack has defined', () => { expect(templatePack).toExist() })
	})
})
